import React from 'react'

const AppSidebar = () => {
  return (
    <aside
      className=''
      // onMouseLeave={() => setIsHovered(false)}
    >

    </aside>
  )
}

export default AppSidebar